---
title: ESP Audio Stack Speaker
description: Speaker platform exposed by ESP Audio Stack.
---

import APIRef from '@components/APIRef.astro';

The `esp_audio_stack` speaker platform writes playback audio into the
full-duplex audio backend owned by the `esp_audio_stack` component. The stack
handles bus-rate conversion, buffering, codec output and the echo-cancellation
reference.

```yaml
esp_audio_stack:
  id: audio_stack
  sample_rate: 48000

speaker:
  - platform: esp_audio_stack
    id: stack_speaker
    esp_audio_stack_id: audio_stack
    buffer_duration: 500ms
```

## Configuration variables

- **esp_audio_stack_id** (**Required**, ID): The parent `esp_audio_stack` component.
- **id** (*Optional*, ID): Manually specify the speaker ID.
- **buffer_duration** (*Optional*, time): Playback staging buffer size.
- **timeout** (*Optional*, time): Stop the speaker after silence. Defaults to no automatic timeout.

## See Also

- [ESP Audio Stack](/components/esp_audio_stack/)
- [Speaker Components](/components/speaker/)

<APIRef text="esp_audio_stack.h" path="esp_audio_stack/esp_audio_stack.h" />
