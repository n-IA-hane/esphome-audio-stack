---
title: ESP Audio Stack Microphone
description: Microphone platform exposed by ESP Audio Stack.
---

import APIRef from '@components/APIRef.astro';

The `esp_audio_stack` microphone platform exposes the capture stream owned by
the `esp_audio_stack` component as a normal ESPHome microphone.

When `processor_id` is configured on the stack, this microphone exposes the
post-processor stream.

```yaml
esp_audio_stack:
  id: audio_stack
  sample_rate: 48000
  output_sample_rate: 16000

microphone:
  - platform: esp_audio_stack
    id: clean_mic
    esp_audio_stack_id: audio_stack
```

## Configuration variables

- **esp_audio_stack_id** (**Required**, ID): The parent `esp_audio_stack` component.
- **id** (*Optional*, ID): Manually specify the microphone ID.

## See Also

- [ESP Audio Stack](/components/esp_audio_stack/)
- [Microphone Components](/components/microphone/)

<APIRef text="esp_audio_stack.h" path="esp_audio_stack/esp_audio_stack.h" />
